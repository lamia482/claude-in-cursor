# Global agent instructions

- Skills live under `~/.agents/skills/` (symlinked to Claude / Cursor / Codex).
- Rules live under `~/.agents/rules/` (see `document-skills.mdc`, `pathology-ml.mdc`).
- MCP: `~/.agents/mcp.json`.
- Sync: `node update.js` from [claude-in-cursor](https://github.com/Yuan1z0825/claude-in-cursor).

For document workflows (papers, patents, grants, PPT), follow `document-skills.mdc` and read the target skill's `SKILL.md` first.
